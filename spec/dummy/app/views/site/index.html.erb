<div><%= t('site.index.footer') %></div>

<div><%= phrase('test', scope: 'models.errors') %></div>

<div><%= phrase 'test', scope: :site %></div>

<div><%= phrase @page, :title %></div>
<div><%= phrase @page, :detail %></div>

<div><%= distance_of_time_in_words(Time.now, Time.now) %></div>
